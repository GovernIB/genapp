<?xml version="1.0" encoding="UTF-8" ?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="${persistencename}" transaction-type="JTA">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <jta-data-source>${jtadatasource}</jta-data-source>

		<#list classes as item>
		<class>${item}</class>
		</#list>

        <!-- Activar cache només per aquelles entitats on volguem expressament -->
        <shared-cache-mode>ENABLE_SELECTIVE</shared-cache-mode>
        <!-- Sempre activar la validació de les propietats -->
        <validation-mode>CALLBACK</validation-mode>

        <properties>
            <property name="hibernate.dialect" value="${dollar}{${package}.hibernate.dialect}" />
            <property name="hibernate.query.substitutions" value="${dollar}{${package}.hibernate.query.substitutions}"/>
        </properties>
    </persistence-unit>
</persistence>