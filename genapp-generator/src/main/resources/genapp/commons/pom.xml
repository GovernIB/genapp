<#assign symbol_dollar = "$">
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>${package}</groupId>
	<artifactId>${name}-utils</artifactId>
	<version>${app_current_version}</version>

	<packaging>jar</packaging>

	<parent>
		<groupId>${package}</groupId>
		<artifactId>${name}</artifactId>
		<version>${app_current_version}</version>
	</parent>

	<name>${fullname} Utils</name>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<version_template_file>./templates/StaticVersion.java.template</version_template_file>
		<version_file>src/main/java/${packagePath}/utils/StaticVersion.java</version_file>
        <project.buildtime>${symbol_dollar}{maven.build.timestamp}</project.buildtime>
        <maven.build.timestamp.format>dd/MM/yyyy HH:mm</maven.build.timestamp.format>
	</properties>


	<build>
		<finalName>${symbol_dollar}{project.artifactId}-${symbol_dollar}{project.version}</finalName>
		<resources>
			<!-- Activam filtre per fer substitucions al fitxer Version.properties -->
			<resource>
				<directory>${symbol_dollar}{project.basedir}/src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>

		<plugins>

			<plugin>
				<groupId>com.google.code.maven-replacer-plugin</groupId>
				<artifactId>maven-replacer-plugin</artifactId>
				<version>1.4.0</version>
				<executions>
					<execution>
						<phase>process-sources</phase>
						<goals>
							<goal>replace</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<file>${symbol_dollar}{version_template_file}</file>
					<outputFile>${symbol_dollar}{version_file}</outputFile>
					<replacements>
						<replacement>
							<token>@project.version@</token>
							<value>${symbol_dollar}{project.version}</value>
						</replacement>
					</replacements>
				</configuration>
			</plugin>
	
			<!-- Defineix la propietat {buildNumber} que s'empra al filtrat -->
			<plugin>
			    <groupId>org.codehaus.mojo</groupId>
			    <artifactId>buildnumber-maven-plugin</artifactId>
			</plugin>
  
		</plugins>

	</build>

	<dependencies>
		<dependency>
			<groupId>javax</groupId>
			<artifactId>javaee-api</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>
	</dependencies>

</project>
