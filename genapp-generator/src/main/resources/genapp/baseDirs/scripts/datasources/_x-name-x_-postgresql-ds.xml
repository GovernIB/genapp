<?xml version="1.0" encoding="UTF-8" ?>
<datasources>
    <local-tx-datasource>
        <jndi-name>${package}.db</jndi-name>
        <connection-url>jdbc:postgresql://localhost:5432/${name}</connection-url>
        <driver-class>org.postgresql.Driver</driver-class>
        <user-name>${name}</user-name>
        <password>${name}</password>
    </local-tx-datasource>
</datasources>