<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>demogenapp</artifactId>
        <groupId>org.fundaciobit.demogenapp</groupId>
        <version>1.0.0</version>
    </parent>
    <artifactId>demogenapp-front</artifactId>
    <packaging>war</packaging>
    <name>DemoGenApp - Front</name>

    <dependencies>
        <dependency>
            <groupId>org.jboss.spec.javax.servlet</groupId>
            <artifactId>jboss-servlet-api_4.0_spec</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <!-- Altres mòduls del projecte -->
        <dependency>
            <groupId>org.fundaciobit.demogenapp</groupId>
            <artifactId>demogenapp-commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ============================================ -->
        <!-- ========== PLUGINS DE LOGIN FRONT =========== -->
        <!-- ============================================ -->

        <dependency>
            <artifactId>pluginsib-login-api</artifactId>
            <groupId>org.fundaciobit.pluginsib.login</groupId>
            <version>${pluginsib.login.version}</version>
        </dependency>
        
        <dependency>
            <artifactId>pluginsib-login-mock</artifactId>
            <groupId>org.fundaciobit.pluginsib.login</groupId>
            <version>${pluginsib.login.version}</version>
        </dependency>
        <dependency>
            <artifactId>pluginsib-login-loginib</artifactId>
            <groupId>org.fundaciobit.pluginsib.login</groupId>
            <version>${pluginsib.login.version}</version>
        </dependency>

        <dependency>
            <artifactId>pluginsib-login-springutils</artifactId>
            <groupId>org.fundaciobit.pluginsib.login</groupId>
            <version>${pluginsib.login.version}</version>
        </dependency>
        
        <dependency>
            <artifactId>pluginsib-core</artifactId>
            <groupId>org.fundaciobit.pluginsib.core.v3</groupId>
            <version>${pluginsib.core.version}</version>
        </dependency>


        <!-- Spring MVC -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aspects</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
            <version>${springsecurity.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
            <version>${springsecurity.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-taglibs</artifactId>
            <version>${springsecurity.version}</version>
        </dependency>

        <!--  Utilitats -->
        <dependency>
            <groupId>org.fundaciobit.genapp</groupId>
            <artifactId>genapp-web</artifactId>
            <version>${genapp.version}</version>
        </dependency>
        <dependency>
            <!--
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            -->
            <groupId>org.jboss.spec.javax.xml.bind</groupId>
            <artifactId>jboss-jaxb-api_2.3_spec</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.activation</groupId>
            <artifactId>activation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.fundaciobit.genapp</groupId>
            <artifactId>genapp-core</artifactId>
            <version>${genapp.version}</version>
        </dependency>
    </dependencies>
    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                  <packagingExcludes>                 
                  </packagingExcludes>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <configuration></configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>src/main/webapp/src</directory>
                            <includes>
                                <include>**/*</include>
                            </includes>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>

        </plugins>

        <resources>
            <resource>
                <directory>${basedir}/src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
        </resources>

    </build>
</project>